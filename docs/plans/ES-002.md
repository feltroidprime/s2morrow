# ES-002: Schema.TaggedError Types and Branded IDs Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Verify that `services/errors.ts` (9 Schema.TaggedError classes) and `services/types.ts` (branded TxHash/ContractAddress + domain types) are fully implemented and tested per PRD requirements.

**Architecture:** All errors use `Schema.TaggedError` from Effect-TS. Branded IDs use `Schema.String.pipe(Schema.brand(...))`. Domain types are plain TypeScript interfaces. Tests use `bun:test` runner with Effect's `runPromiseExit` for failure roundtrip assertions.

**Tech Stack:** Effect-TS (Schema.TaggedError, Schema.brand), Bun test runner, TypeScript strict mode

---

## Status: ALREADY IMPLEMENTED

Research confirms all acceptance criteria are met. This plan documents what exists and the verification steps to confirm compliance.

### What Already Exists

**`apps/demo/src/services/errors.ts`** — 9 Schema.TaggedError classes:

| Error Class | Fields | PRD Requirement |
|-------------|--------|-----------------|
| `WasmLoadError` | `message: String` | ✅ WASM module load failure |
| `KeygenError` | `message: String` | ✅ Keypair generation failure |
| `SigningError` | `message: String` | ✅ Message signing failure |
| `VerificationError` | `message: String, step: String` | ✅ Verification failure with step context |
| `HintGenerationError` | `message: String` | ✅ Hint generation failure |
| `PackingError` | `message: String` | ✅ Packing failure |
| `StarknetRpcError` | `message: String, code: Number` | ✅ RPC failure with error code |
| `AccountDeployError` | `message: String, txHash: optional(String)` | ✅ Deploy failure with optional tx hash |
| `InsufficientFundsError` | `message: String, address: String, required: String` | ✅ Funding check failure |

**`apps/demo/src/services/types.ts`** — Branded IDs and domain types:

| Type | Pattern | PRD Requirement |
|------|---------|-----------------|
| `TxHash` | `Schema.String.pipe(Schema.brand("@Falcon/TxHash"))` | ✅ Branded transaction hash |
| `ContractAddress` | `Schema.String.pipe(Schema.brand("@Falcon/ContractAddress"))` | ✅ Branded contract address |
| `FalconKeypair` | Interface: `secretKey`, `verifyingKey`, `publicKeyNtt` | ✅ Keypair domain type |
| `FalconSignatureResult` | Interface: `signature`, `salt` | ✅ Signature result type |
| `PackedPublicKey` | Interface: `slots: ReadonlyArray<string>` | ✅ Packed PK type |
| `VerificationStep` | Discriminated union (8 variants) | ✅ Verification flow state |
| `PipelineStep` | Interface: `id`, `name`, `description`, `input`, `output`, `stepCount`, `status` | ✅ Pipeline step type |

**Test files:**

| File | Tests | Status |
|------|-------|--------|
| `src/__tests__/verify-playground/errors.test.ts` | 15 tests — WasmLoadError, KeygenError, SigningError, VerificationError, HintGenerationError, PackingError | ✅ All pass |
| `src/__tests__/effect-services/starknet-errors.test.ts` | 12 tests — StarknetRpcError, AccountDeployError, InsufficientFundsError | ✅ All pass |

**Total: 27/27 tests pass.**

---

## Task 1: Verify Existing Implementation (Confirmation Only)

No code changes needed. This task confirms the implementation meets all acceptance criteria.

**Files:**
- Read: `apps/demo/src/services/errors.ts`
- Read: `apps/demo/src/services/types.ts`
- Test: `apps/demo/src/__tests__/verify-playground/errors.test.ts`
- Test: `apps/demo/src/__tests__/effect-services/starknet-errors.test.ts`

**Step 1: Run all error tests**

Run: `cd apps/demo && bun test src/__tests__/verify-playground/errors.test.ts src/__tests__/effect-services/starknet-errors.test.ts`
Expected: 27 pass, 0 fail

**Step 2: Verify errors.ts has all 9 TaggedError classes**

Check that `errors.ts` contains exactly these 9 classes:
1. `WasmLoadError` — `{ message: Schema.String }`
2. `KeygenError` — `{ message: Schema.String }`
3. `SigningError` — `{ message: Schema.String }`
4. `VerificationError` — `{ message: Schema.String, step: Schema.String }`
5. `HintGenerationError` — `{ message: Schema.String }`
6. `PackingError` — `{ message: Schema.String }`
7. `StarknetRpcError` — `{ message: Schema.String, code: Schema.Number }`
8. `AccountDeployError` — `{ message: Schema.String, txHash: Schema.optional(Schema.String) }`
9. `InsufficientFundsError` — `{ message: Schema.String, address: Schema.String, required: Schema.String }`

**Step 3: Verify types.ts has branded IDs and domain types**

Check that `types.ts` contains:
- `TxHash` branded schema + type export
- `ContractAddress` branded schema + type export
- `FalconKeypair` interface
- `FalconSignatureResult` interface
- `PackedPublicKey` interface
- `VerificationStep` discriminated union
- `PipelineStep` interface

**Step 4: Verify PRD naming compliance**

Per PRD: "VerificationError with step field" — confirmed, the error has `{ message: Schema.String, step: Schema.String }`.
Per PRD: Error naming uses `VerificationError` (not `VerificationFailedError` from the design doc) — confirmed.

**Step 5: Verify Effect best practices compliance**

- [x] All errors use `Schema.TaggedError` (not plain classes)
- [x] All errors have a `message: Schema.String` field
- [x] Context-specific fields present: `step` on VerificationError, `code` on StarknetRpcError, `txHash` on AccountDeployError, `address`+`required` on InsufficientFundsError
- [x] Branded types use `Schema.String.pipe(Schema.brand(...))` pattern
- [x] Brand prefixes use `@Falcon/` namespace

---

## Acceptance Criteria Checklist

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 9 Schema.TaggedError error classes in errors.ts | ✅ Met | All 9 classes present with correct fields |
| All errors constructable | ✅ Met | 27 tests verify construction |
| All errors _tag matchable | ✅ Met | Tests verify `catchTag` pattern |
| VerificationError has step field | ✅ Met | `step: Schema.String` field confirmed |
| Branded TxHash in types.ts | ✅ Met | `Schema.String.pipe(Schema.brand("@Falcon/TxHash"))` |
| Branded ContractAddress in types.ts | ✅ Met | `Schema.String.pipe(Schema.brand("@Falcon/ContractAddress"))` |
| Domain types (FalconKeypair, etc.) | ✅ Met | All interfaces present |
| Unit tests for each error type | ✅ Met | 27 tests across 2 files, all passing |

---

## Risks

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Tests break on Effect version bump | Low | Pinned `effect: ^3.13.0` in package.json |
| Brand namespace collision | Very Low | Using `@Falcon/` prefix, unique to this app |

---

## Conclusion

**No implementation work needed.** ES-002 is fully complete. The ticket can be closed after running verification (Task 1) to confirm all 27 tests still pass on the current branch.

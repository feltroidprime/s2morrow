IMPORTANT: After completing the task below, you MUST output a JSON object in a ```json code fence at the very end of your response. Do NOT forget this — the workflow fails without it.

CODE QUALITY REVIEW — Ticket: LANDING-003

Title: Implement WhyPostQuantum RSC section with 4 explanation cards
Category: landing

You are reviewing this ticket&#x27;s implementation for **code quality only**.
Spec compliance is reviewed separately — focus here on quality, correctness patterns, and maintainability.

## Files to Review

Created:
- apps/demo/src/components/landing/WhyPostQuantum.tsx
Modified:
- apps/demo/src/tests/landing/why-post-quantum.test.ts
- apps/demo/src/app/page.tsx
## Review Checklist

READ every file modified/created. Check:

### Code quality
- Check error handling
- Check security
- Check code style
- Check test coverage
- Check performance
- Check architecture
Severity levels:

- &quot;critical&quot; — Security issue, data corruption risk, or missing error handling

- &quot;major&quot; — Missing tests, architectural problems, or significant quality issues

- &quot;minor&quot; — Style issues, minor refactoring opportunities

- &quot;none&quot; — Code is production-quality

Use severity &quot;none&quot; only if there are absolutely no quality issues.


**REQUIRED OUTPUT** — You MUST end your response with a JSON object in a code fence matching this schema:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "severity": {
      "type": "string",
      "enum": [
        "none",
        "minor",
        "major",
        "critical"
      ]
    },
    "feedback": {
      "type": "string"
    },
    "issues": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "severity",
    "feedback",
    "issues"
  ],
  "additionalProperties": false
}
```
Output the JSON at the END of your response. The workflow will fail without it.
make deploy works but the wasm module bug ("WASM module is missing required methods. Expected: keygen, sign, verify, create_verification_hint, ntt_public_key, pack_public_key_wasm, public_key_length, salt_length, sign_for_starknet.

") . Fix the make deploy command.

---

[Request interrupted by user]

---

theres nothing to fix it's only aboyt the build command